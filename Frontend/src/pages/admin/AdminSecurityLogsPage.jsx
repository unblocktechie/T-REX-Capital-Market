import { useQuery } from '@tanstack/react-query';
import { KeyRound, ShieldAlert, ShieldCheck } from 'lucide-react';
import { adminApi } from '@/api/admin';
import { AdminPanel } from '@/components/admin/AdminPanel';
import { useDocumentTitle } from '@/hooks/useDocumentTitle';
import { formatAdminDateTime } from '@/utils/adminFormat';
export default function AdminSecurityLogsPage(){useDocumentTitle('Security Logs');const logs=useQuery({queryKey:['admin','security-logs'],queryFn:adminApi.listSecurityLogs});return <div className="space-y-6"><header><p className="mb-2 text-[11px] font-semibold tracking-[0.16em] text-blue-600 uppercase dark:text-blue-400">Privileged access monitoring</p><h2 className="m-0 text-3xl font-semibold tracking-normal text-slate-950 sm:text-4xl dark:text-white">Security Logs</h2><p className="mt-3 mb-0 text-sm leading-6 text-slate-500 dark:text-slate-400">Monitor administrator authentication, devices, and security-sensitive events.</p></header><AdminPanel title="Access activity" description="Recent privileged account activity." action={<KeyRound className="size-5 text-slate-400" />}><div className="space-y-3">{logs.isLoading?Array.from({length:3}).map((_,i)=><div key={i} className="h-20 animate-pulse rounded-2xl bg-slate-100 dark:bg-slate-800"/>):(logs.data||[]).map((log)=><article key={log.id} className="grid gap-3 rounded-2xl border border-slate-200 p-4 md:grid-cols-[minmax(0,1fr)_180px_180px] md:items-center dark:border-slate-800"><div className="flex min-w-0 items-center gap-3"><span className={`grid size-10 shrink-0 place-items-center rounded-xl ${log.status==='success'?'bg-emerald-50 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-300':log.status==='blocked'?'bg-rose-50 text-rose-700 dark:bg-rose-500/10 dark:text-rose-300':'bg-amber-50 text-amber-700 dark:bg-amber-500/10 dark:text-amber-300'}`}>{log.status==='success'?<ShieldCheck className="size-5"/>:<ShieldAlert className="size-5"/>}</span><div className="min-w-0"><strong className="block truncate text-sm text-slate-950 dark:text-white">{log.event}</strong><p className="mt-1 mb-0 truncate text-xs text-slate-500">{log.actor} · {log.location}</p></div></div><div><span className="block text-[10px] font-bold text-slate-400 uppercase">Device</span><strong className="mt-1 block text-xs text-slate-700 dark:text-slate-300">{log.device}</strong></div><time className="text-xs font-semibold text-slate-500 md:text-right">{formatAdminDateTime(log.at)}</time></article>)}</div></AdminPanel></div>}
