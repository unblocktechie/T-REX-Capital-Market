# Issuer dashboard metric card spacing

- Vertically centers metric-card icon, copy, and navigation affordance.
- Reduces excessive minimum card height while preserving room for wrapped helper text.
- Uses a slightly tighter mobile minimum height so single-column cards stay compact and readable.
- Change is scoped to issuer dashboard metric cards only; dashboard data and navigation behavior are unchanged.
