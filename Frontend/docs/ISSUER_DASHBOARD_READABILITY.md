# Issuer Dashboard Readability Update

Updated the live issuer dashboard presentation without changing its data sources, navigation, or business logic.

- Increased small supporting text across metric cards, token snapshot, workflow, action center, and recent activity.
- Increased important card headings and metric values while preserving the existing visual hierarchy.
- Increased row height where needed so larger type does not crowd interactive dashboard rows.
- Kept the four-column desktop metric layout, two-column tablet layout, and single-column mobile layout.
- Scoped typography overrides to the issuer dashboard so other portal screens retain their existing styling.
